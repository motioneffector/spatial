/**
 * Spatial graph implementation - stub for TDD
 */

import type { SpatialGraph, SpatialGraphOptions } from './types'

/**
 * Creates a new spatial graph instance
 *
 * @param options - Configuration options
 * @returns A new SpatialGraph instance
 */
export function createSpatialGraph(_options?: SpatialGraphOptions): SpatialGraph {
  // Stub - will be implemented after tests
  throw new Error('Not implemented')
}
